# Source snapshot

The `src/` and numbered method scripts in this isolated integration were copied
from the sibling `my_method/my_method` repository at commit:

```text
3d9085e3d7ea6faa5d3ff1d79ece3993b17964a9
```

SafeEraser core files are not imported into or modified by the method. The only
shared interfaces are the paired dataset, a merged Hugging Face model directory,
and SafeEraser's existing command-line inference/evaluation programs.
